

Підготовка проєкту:
 -Встановив tailwind CSS
 -Встановив react-router-dom
 -Встановив tailwind-merge, clsx, class-variance-authority
 -Підключив шрифти Rubik & Karla в index.html

навигации:
 Створив роут для сторінку Edit User
 Створив роут для сторінку Users

Компоненти:
 -Створив Header Для навигации між сторинками
 -Створив кнопку для перевикористання її в інших місцях проєкту


задача створити Edit User сторінку:
 -Опис:
    Сторінка для редагування даних юзера.
    В селекторі User обираємо і, знизу, підвантажуються дані котрі можна редагувати. 
    Кнопку Undo, по-дефолту, не видно. Зʼявляється тільки тоді, коли ми щось змінюємо. 
    Коли натискаємо на неї - дані скидаються як були до змін. 
    Кнопка Save стає активною коли дані змінені і є, що зберігати.

 -Верстка сторінки
 -створити мультисеклектор щоб можна було його перевикористовувати в інших місцях проєкту
 -при вибори користувача щоб знизу зявлялась информация про нього
 -Кнопку Undo, по-дефолту, не видно. Зʼявляється тільки тоді, коли ми щось змінюємо. 
  Коли натискаємо на неї - дані скидаються як були до змін.
 -Кнопка Save стає активною коли дані змінені і є, що зберігати.


задача створити Users сторінку:
 -Опис:
   Поки не обрано три департаменти - інші фільтри деактивовані і не можуть бути змінені. 
   Якщо інші фільтри обрані, ми видаляємо департамент, їх стає менше двох - інші фільтри скидаються до дефолту і деактивуються. 
   Смітничок біля фільтрів зкидає всі поля до дефолтних значеньє
   Смітничок біля юзер-айтема у списку видаляє юзера
   Add User відкриває попап додавання юзера

 -Верстка сторінки

Створити add new User Modal:
  -верстка модельного окна
  -відкриття за закриття модельного окна
  -додати форму для додавання нового користувача
